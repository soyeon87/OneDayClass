apiVersion: v1
kind: Service
metadata:
  name: user-reservation
  namespace: onedayclass
  labels:
    app: user-reservation
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: user-reservation