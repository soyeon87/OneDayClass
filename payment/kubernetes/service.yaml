apiVersion: v1
kind: Service
metadata:
  name: user-payment
  namespace: onedayclass
  labels:
    app: user-payment
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: user-payment