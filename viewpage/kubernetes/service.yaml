apiVersion: v1
kind: Service
metadata:
  name: user03-viewpage
  namespace: onedayclass
  labels:
    app: user03-viewpage
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: user03-viewpage