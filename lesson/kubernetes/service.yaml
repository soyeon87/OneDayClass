apiVersion: v1
kind: Service
metadata:
  name: user-lesson
  namespace: onedayclass
  labels:
    app: user-lesson
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: user-lesson